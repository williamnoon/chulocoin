{
  "buildCommand": "cd ../.. && npm install && cd frontend/landing && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --prefix=../.. && npm install",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}
