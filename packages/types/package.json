{
  "name": "@chulobots/types",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'No source files to lint in types package'",
    "clean": "rm -rf dist"
  }
}
